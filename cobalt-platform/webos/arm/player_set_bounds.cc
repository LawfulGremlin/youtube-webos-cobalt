#include "starboard/player.h"

// Stage 2 presents only Cobalt's GLES UI. Starfish punch-out bounds are added
// with the player implementation in Stage 3.
void SbPlayerSetBounds(SbPlayer, int, int, int, int, int) {}
