diff --git a/starboard/linux/shared/BUILD.gn b/starboard/linux/shared/BUILD.gn
--- a/starboard/linux/shared/BUILD.gn
+++ b/starboard/linux/shared/BUILD.gn
@@ -406,8 +406,6 @@ static_library("starboard_platform_sources") {
   # newer BTN_* definitions.
   if (target_platform == "webos-arm") {
     sources -= [
-      "//starboard/shared/libdav1d/dav1d_video_decoder.cc",
-      "//starboard/shared/libdav1d/dav1d_video_decoder.h",
       "//starboard/shared/linux/dev_input/dev_input.cc",
     ]
   }
diff --git a/starboard/linux/shared/player_components_factory.cc b/starboard/linux/shared/player_components_factory.cc
--- a/starboard/linux/shared/player_components_factory.cc
+++ b/starboard/linux/shared/player_components_factory.cc
@@ -20,9 +20,7 @@
 #include "starboard/media.h"
 #include "starboard/shared/ffmpeg/ffmpeg_audio_decoder.h"
 #include "starboard/shared/ffmpeg/ffmpeg_video_decoder.h"
-#if !defined(STARBOARD_WEBOS)
 #include "starboard/shared/libdav1d/dav1d_video_decoder.h"
-#endif
 #include "starboard/shared/libvpx/vpx_video_decoder.h"
 #include "starboard/shared/libde265/de265_video_decoder.h"
 #include "starboard/shared/libfdkaac/fdk_aac_audio_decoder.h"
@@ -105,9 +103,7 @@ class PlayerComponentsFactory : public PlayerComponents::Factory {
     }
 
     if (creation_parameters.video_codec() != kSbMediaVideoCodecNone) {
-#if !defined(STARBOARD_WEBOS)
       typedef ::starboard::shared::libdav1d::VideoDecoder Av1VideoDecoderImpl;
-#endif
       typedef ::starboard::shared::vpx::VideoDecoder VpxVideoDecoderImpl;
       typedef ::starboard::shared::de265::VideoDecoder H265VideoDecoderImpl;
       typedef ::starboard::shared::ffmpeg::VideoDecoder FfmpegVideoDecoderImpl;
@@ -122,22 +118,16 @@ class PlayerComponentsFactory : public PlayerComponents::Factory {
 
       video_decoder->reset();
 
-#if !defined(STARBOARD_WEBOS)
-      const SbMediaVideoCodec kAv1VideoCodec = kSbMediaVideoCodecAv1;
-#endif
-
       if (creation_parameters.video_codec() == kSbMediaVideoCodecVp9) {
         video_decoder->reset(new VpxVideoDecoderImpl(
             creation_parameters.video_codec(),
             creation_parameters.output_mode(),
             creation_parameters.decode_target_graphics_context_provider()));
-#if !defined(STARBOARD_WEBOS)
-      } else if (creation_parameters.video_codec() == kAv1VideoCodec) {
+      } else if (creation_parameters.video_codec() == kSbMediaVideoCodecAv1) {
         video_decoder->reset(new Av1VideoDecoderImpl(
             creation_parameters.video_codec(),
             creation_parameters.output_mode(),
             creation_parameters.decode_target_graphics_context_provider()));
-#endif
       } else if (creation_parameters.video_codec() == kSbMediaVideoCodecH265) {
         video_decoder->reset(new H265VideoDecoderImpl(
             creation_parameters.video_codec(),
diff --git a/starboard/linux/shared/media_is_video_supported.cc b/starboard/linux/shared/media_is_video_supported.cc
--- a/starboard/linux/shared/media_is_video_supported.cc
+++ b/starboard/linux/shared/media_is_video_supported.cc
@@ -62,9 +62,9 @@ bool SbMediaIsVideoSupported(SbMediaVideoCodec video_codec,
 
   return (
 #if defined(STARBOARD_WEBOS)
-             // The TV FFmpeg runtime decodes AVC and the port ships a small
-             // VP9-only libvpx. Do not advertise AV1 until a matching decoder
-             // is present, otherwise YouTube selects it and playback fails.
+             // Keep software codec support explicit: FFmpeg handles AVC,
+             // VP9-only libvpx handles VP9 and dav1d handles AV1.
+             video_codec == kSbMediaVideoCodecAv1 ||
              video_codec == kSbMediaVideoCodecH264 ||
              video_codec == kSbMediaVideoCodecVp9
 #else
