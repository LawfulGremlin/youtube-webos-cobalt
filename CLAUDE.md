# youtube-webos-cobalt

**The project rules live in [AGENTS.md](AGENTS.md). Read it — it is imported below, but if the
import ever fails to resolve, open it directly. Do not act on this repository without it.**

Change a rule in `AGENTS.md`. Never copy the rules back into this file.

@AGENTS.md
